# 📖 使用示例

## 🎯 基本用法

### 普通模式

```
你: /sys ls
Bot: [/home/user]
     file1.txt
     file2.log
     README.md

你: /sys cd /var/log
Bot: 切换到: /var/log

你: /sys ls
Bot: [/var/log]
     nginx/
     syslog
     auth.log
```

---

## ⚡ 直接模式（推荐）

**进入直接模式后，不需要每次都输入 `/sys`**

```
你: /sys >>
Bot: 已进入直接模式
     当前目录: /home/user
     发送 << 退出

你: ls
Bot: [/home/user]
     file1.txt
     file2.log

你: cd /tmp
Bot: 切换到: /tmp

你: pwd
Bot: /tmp

你: ls
Bot: [/tmp]
     temp1
     temp2

你: <<
Bot: 已退出直接模式
```

**直接模式下的所有命令都会显示当前路径 `[path]`**

---

## 📂 目录管理

### 切换目录

```
/sys cd /var/log      # 绝对路径
/sys cd nginx         # 相对路径
/sys cd ..            # 上级目录
/sys cd ~             # 用户主目录
/sys cd /             # 根目录
```

### 查看和重置

```
/sys pwd              # 显示当前目录
/sys reset            # 重置回初始目录
```

---

## 📜 命令历史

```
你: /sys ls
你: /sys df -h
你: /sys ps aux

你: /sys history
Bot: 命令历史 (最近10条):
     1. ls
     2. df -h
     3. ps aux
```

---

## 💪 强制全部输出

### 普通执行（自动截断）

```
你: /sys cat large.log
Bot: [/var/log]
     line 1
     line 2
     ...
     (已截断，完整10000字符，使用 -f 强制全部输出)
```

### 使用 -f 参数

```
你: /sys cat large.log -f
Bot: [/var/log]
     line 1
     line 2
     ... (全部10000字符都显示)
```

---

## 🚫 交互式命令处理

### 被拦截的命令

```
你: /sys vim config.txt
Bot: 不支持 vim (查看文件)
     替代: cat <文件>

你: /sys top
Bot: 不支持 top (查看进程)
     替代: top -bn1 | head -20 或 ps aux
```

### 正确用法

```
你: /sys cat config.txt
你: /sys top -bn1 | head -20
你: /sys ps aux
```

---

## 🎨 智能输出优化

### df -h (磁盘使用)

```
你: /sys df -h
Bot: [/home/user]
     磁盘使用:
     /: 45% (20G/50G, 剩余30G)
     /home: 82% (80G/100G, 剩余20G)
     /data: 95% (450G/500G, 剩余50G)
```

### free -h (内存使用)

```
你: /sys free -h
Bot: [/home/user]
     内存:
     总量: 16Gi, 已用: 8Gi, 可用: 8Gi
     交换: 4Gi (已用: 512Mi)
```

### ps aux (进程列表)

```
你: /sys ps aux
Bot: [/home/user]
     进程 (前10条):
     python app.py (CPU:12.5% MEM:3.8%)
     nginx (CPU:0.5% MEM:1.2%)
     ...
```

---

## 🔄 直接模式 vs 普通模式

### 普通模式
- 每次都要输入 `/sys`
- 适合偶尔执行几个命令

```
/sys ls
/sys cd /tmp
/sys ls
```

### 直接模式（推荐批量操作）
- 不需要输入 `/sys`
- 适合连续执行多个命令
- 像真正的SSH一样

```
/sys >>              # 进入直接模式
ls
cd /tmp
ls
df -h
free -h
pwd
<<                   # 退出直接模式
```

---

## 💡 实用技巧

### 1. 批量操作

**直接模式最方便：**
```
/sys >>
cd /var/log/nginx
tail -100 access.log
grep "404" access.log
wc -l access.log
<<
```

### 2. 查找文件

```
/sys find /home -name "*.log"
/sys locate config.txt
/sys which python
```

### 3. 监控系统

```
/sys >>
df -h
free -h
ps aux
netstat -antp
uptime
<<
```

### 4. 日志查看

```
/sys tail -100 /var/log/syslog
/sys grep "error" /var/log/nginx/error.log
/sys journalctl -n 50
```

---

## ⚠️ 注意事项

1. **每条命令都显示路径** - `[/current/path]`
2. **直接模式下避免发送非命令消息** - 会被当作命令执行
3. **使用 `/` 开头的消息会跳过** - 保持其他插件正常工作
4. **-f 参数慎用** - 可能导致超长输出

---

**开始体验吧！** 🚀

