{
  "sysmanager": {
    "description": "🖥️ 服务器命令执行配置",
    "type": "object",
    "items": {
      "command_timeout": {
        "description": "⏱️ 命令执行超时时间（秒）",
        "type": "int",
        "default": 30,
        "hint": "执行命令的最长等待时间，超过将中止。建议：10-60秒"
      },
      "max_output_length": {
        "description": "📏 最大输出长度（字符）",
        "type": "int",
        "default": 2000,
        "hint": "命令输出的最大字符数，超过将截断。建议：1000-3000"
      },
      "log_operations": {
        "description": "📝 记录操作日志",
        "type": "bool",
        "default": true,
        "hint": "是否记录所有执行的命令，强烈建议开启"
      }
    }
  }
}
